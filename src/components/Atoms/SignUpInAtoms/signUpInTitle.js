import { Typography } from '@material-ui/core';

const RegAndLogTitle = ({title}) => {
    return (
        <Typography component='h1' variant='h5'>
            {title}
        </Typography>
    )
};

export default RegAndLogTitle;
